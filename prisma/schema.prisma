// =====================================================
// Prisma schema placeholder
// -----------------------------------------------------
// このファイルは npm scripts により環境ごとの schema で上書きされます。
// - 開発: prisma/schema.dev.prisma （SQLite）
// - 本番: prisma/schema.prod.prisma（PostgreSQL）
// 直接このファイルを編集しないでください。
// エディタ補完用に最小限の定義のみ保持しています。
// =====================================================

generator client {
  provider = "prisma-client-js"
}

// ダミーのdatasource（実行時はスクリプトで上書き）
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 実体のモデル定義は schema.dev.prisma / schema.prod.prisma を参照
