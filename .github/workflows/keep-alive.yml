name: Keep Supabase Active

on:
  schedule:
    # 毎週月曜日、水曜日、土曜日 0:00 UTC (日本時間 9:00) に実行
    # 週3回実行することで、最大2-3日間隔でSupabaseへアクセスし、7日間の自動停止を防ぐ
    # Vercelのコールドスタート問題にも対応するため、より頻繁にアクセス
    - cron: '0 0 * * 1,3,6'

  # 手動実行も可能にする
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping health endpoint
        env:
          HEALTH_CHECK_URL: ${{ secrets.PRODUCTION_URL }}
        run: |
          # 環境変数が設定されていない場合はエラー
          if [ -z "$HEALTH_CHECK_URL" ]; then
            echo "Error: PRODUCTION_URL secret is not set"
            echo "Please set PRODUCTION_URL in repository secrets"
            exit 1
          fi

          response=$(curl -s -w "\n%{http_code}" "${HEALTH_CHECK_URL}/api/health")
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n-1)

          echo "Target URL: ${HEALTH_CHECK_URL}/api/health"
          echo "HTTP Status: $http_code"
          echo "Response: $body"

          if [ "$http_code" -ne 200 ]; then
            echo "Health check failed with status $http_code"
            exit 1
          fi

          echo "Health check successful!"
